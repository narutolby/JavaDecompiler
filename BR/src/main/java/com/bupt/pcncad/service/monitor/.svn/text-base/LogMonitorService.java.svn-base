package com.bupt.pcncad.service.monitor;

import com.bupt.pcncad.monitor.AbstractMonitor;
import com.bupt.pcncad.monitor.log.LogMonitor;
import com.bupt.pcncad.webContext.BRWebApplicationContext;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;

import javax.servlet.ServletContextEvent;
import java.io.*;

/**
 * Created with IntelliJ IDEA.
 * User: liboyang01
 * Date: 12-9-29
 * Time: 下午3:12
 * To change this template use File | Settings | File Templates.
 */
/*@Service*/
public class LogMonitorService {


   /* @Value("${monitorLogPath}")
    private void setMonitorLogPath(String monitorLogPath) throws Exception{
        this.logMonitor.setLogPath(monitorLogPath);

    }

    private LogMonitor logMonitor = new LogMonitor();


    public synchronized void start(boolean first) throws Exception {

        this.logMonitor.on(first);
    }


    public void stop() throws Exception {
        if (this.logMonitor.isOn()) {
            this.logMonitor.off();
        }
    }

    public String getLog() {
        return this.logMonitor.getLog();
    }
*/
}
